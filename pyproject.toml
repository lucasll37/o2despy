[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "o2despy"
version = "0.1.0"
description = "Object-Oriented Discrete Event Simulation framework"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "numpy",
    "pandas",
    "sortedcontainers",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["o2despy*", "commons*"]